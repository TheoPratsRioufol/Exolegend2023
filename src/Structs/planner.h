// #ifndef PLANNER_H
// #define PLANNER_H

// #include <iostream>
// #include <vector>
// #include <queue>
// #include <cmath>
// #include <algorithm>

// struct Node {
//     int x, y;
//     int g, h, f;
//     Node* parent;
    
//     Node(int x, int y, int g, int h, Node* parent = nullptr);
// };

// struct CompareNodes {
//     bool operator()(const Node* a, const Node* b);
// };

// int heuristic(int x1, int y1, int x2, int y2);

// std::vector<std::pair<int, int>> findPath(std::vector<std::vector<int>>& maze, std::pair<int, int> start, std::pair<int, int> goal);

// void printMaze(std::vector<std::vector<int>>& maze, const std::vector<std::pair<int, int>>& path);

// #endif // PLANNER_H
